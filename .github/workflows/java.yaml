name: Java-workflow1

on:
  push: 
    branches:
      - main 

jobs: 
  build:
    name: my-spc
    runs-on: self-hosted  

    steps:
      - name: git checkout
        uses: actions/checkout@v4

      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          java-version: '25'
          distribution: 'ubuntu'

      - name: build Java with maven
        run: mvn package 